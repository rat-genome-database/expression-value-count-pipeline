8/7/2026
 changed expression level to 'all' -- no longer computing 'below cutoff', 'low', 'medium', high'
 unchanged counts now count towards the periodic flush; previously they accumulated for a whole species
   because the flush only looked at the new and changed lists
 counts are now computed by a single aggregate query per species and level, instead of one query per gene
   per slim term; the per-gene loop, the parallel stream over terms and the JDBC retry are gone with it
 added memory usage monitoring; updated rgdcore
 summary now reports counts corrected (stored value disagreed with the recomputed one) separately from
   counts merely touched, per level and per species, with a single run-level block at the end
 rows whose gene/term pair no longer has any expression values are now deleted instead of keeping their old
   count forever; guarded by deleteThresholdPercent (default 5%) so a short aggregate cannot empty the table
 added inserted.log, corrected.log and deleted.log listing the rows affected

7/14/2025
 adding an update for the "Failed to obtain JDBC Connection" error that is causing a timeout, "Timeout waiting for idle object"

10/14/2024
updating to run a parallelStream for uberon terms

10/04/2024
created pipeline to generate counts for expression values